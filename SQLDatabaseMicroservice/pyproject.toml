[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "football-repository"
version = "0.1.0"
description = "Football SQLite repository"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]